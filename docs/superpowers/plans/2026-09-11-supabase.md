# Supabase accounts and progress

Approved specification: the implementation request in this task. Execute inline, without approval gates. Static HTML and public lesson/quiz contracts remain unchanged.

1. Add failing Node/browser tests for an injectable Supabase client, local import, cloud hydration, account separation, durable pending writes, notes and authentication. Run against the missing APIs first.
2. Create a reproducible SQL migration with three owner-scoped tables, explicit grants, RLS for every operation, bounded payloads and server timestamps. Execute it in embedded PostgreSQL and test two identities and anonymous access.
3. Add a pinned local SDK bundle and public empty configuration. Build a safe storage adapter before existing consumers; preserve raw study/quiz cache keys and keep owner-specific snapshots plus a durable outbox. Restore guest data on logout and retain unsent account data only in that account's vault.
4. Extend study and quiz persistence through the adapter. Synchronize via debounced serial upserts; read all cloud tables before imports; retain conflicts for export and reject stale identity responses. Isolate existing local analytics by identity; history stays local and is not synchronized.
5. Add common Romanian account UI and section-specific notes. Use native dialogs for protected mobile focus and a nonmodal desktop notes panel. Save input locally immediately, capture chapter/section before scheduling writes, preserve drafts on close/navigation.
6. Wire all public documents and a static auth callback page. Regenerate bundle/precache; bypass auth/data requests in the worker. Document deployment, redirects, SMTP, RLS checks, export and conflict policy.
7. Run mock integration tests, embedded SQL tests, existing full npm suite and diff checks. Inspect desktop/mobile screenshots. Report actual coverage and dashboard steps, without claiming a live Supabase connection.
